# explainable-chronos

chronos big

run python run_extensions.py ext1 evaluate to evaluate extension one

make forecast generation more realistic

next step use another LLM which is not gemma because we cannot change transformer version
otherwise chronos break down

so run eval on LLM with RST guided and then another LLM which isn't RST guided a
compare the results and see if RST is helpful or not