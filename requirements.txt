# explainable-chronos — pinned dependencies
# Install with: pip install -r requirements.txt
# Or (recommended for Colab): pip install -e .

# Core scientific stack
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scipy>=1.10,<2.0
scikit-learn>=1.3
matplotlib>=3.7,<4.0

# PyTorch
# On Colab with GPU: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0,<3.0

# Transformers — 4.44+ required for Qwen2.5-7B-Instruct
transformers>=4.44,<5.0

# Efficient model loading / fp16 helpers
accelerate>=0.30

# Chronos-2 backbone
chronos-forecasting>=2.0


# Dataset utilities
yfinance>=0.2.0
datasets>=2.18.0

# Development
pytest>=7.4,<9.0
